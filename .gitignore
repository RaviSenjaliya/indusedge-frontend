node_modules
dist
.vscode

# Secrets / local overrides — never commit. Template (.env.example) is tracked.
.env
.env.local
.env.*.local
!.env.example

# Lockfile IS committed: Render runs `npm ci`, which requires it.

*.log
npm-debug.log*
